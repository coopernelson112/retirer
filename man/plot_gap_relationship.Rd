% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gap_relationship.R
\name{plot_gap_relationship}
\alias{plot_gap_relationship}
\title{Plot the cross-sectional relationship between two sex gaps}
\usage{
plot_gap_relationship(
  x_measure = "LE65",
  y_measure = "retirement_age",
  year = NULL,
  level = c("group", "country"),
  groups = NULL,
  countries = NULL,
  gap = "women_minus_men",
  show_labels = FALSE,
  add_smooth = TRUE
)
}
\arguments{
\item{x_measure}{Character scalar naming the sex-comparable measure to place
on the x-axis.}

\item{y_measure}{Character scalar naming the sex-comparable measure to place
on the y-axis.}

\item{year}{Optional numeric scalar giving the year to plot. If \code{NULL}, the
latest year with overlapping non-missing gap data for both measures and the
requested selection is used.}

\item{level}{Character scalar indicating the level to plot. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to plot when
\code{level = "group"}. If \code{NULL}, all four membership groups are plotted.}

\item{countries}{Optional character vector of country names to plot when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible. For readability, country-level selections are
limited to 6 countries.}

\item{gap}{Character scalar indicating the direction of the gap. Must be one
of \code{"women_minus_men"} or \code{"men_minus_women"}.}

\item{show_labels}{Logical scalar indicating whether to label the points with
\code{series_label}.}

\item{add_smooth}{Logical scalar indicating whether to add a least-squares
regression line.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot the directed sex gap in one measure against the directed sex gap in
another measure for a single year, either across OECD/EU membership groups or
across selected countries.
}
\examples{
plot_gap_relationship()

plot_gap_relationship(
  x_measure = "LE65",
  y_measure = "retirement_age",
  year = 2020
)

plot_gap_relationship(
  x_measure = "Q1550",
  y_measure = "retirement_age",
  level = "country",
  countries = c("China", "Japan", "Germany"),
  gap = "men_minus_women",
  show_labels = TRUE,
  add_smooth = FALSE
)

}
