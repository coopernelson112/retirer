% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_retirement_trends.R
\name{plot_retirement_trends}
\alias{plot_retirement_trends}
\title{Plot retirement-age trends over time}
\usage{
plot_retirement_trends(
  measure = "retirement_age",
  sex = c("both", "women", "men"),
  level = c("group", "country"),
  groups = NULL,
  countries = NULL,
  show_points = TRUE
)
}
\arguments{
\item{measure}{Character scalar naming the measure to plot on the y-axis.
Must be \code{"retirement_age"}.}

\item{sex}{Character scalar indicating the sex-specific version to use.
Must be one of \code{"both"}, \code{"women"}, or \code{"men"}. \code{"both"} plots the
row-wise average of \code{ave_age_women} and \code{ave_age_men}.}

\item{level}{Character scalar indicating the level to plot. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to plot when
\code{level = "group"}. If \code{NULL}, all four membership groups are plotted:
\code{"OECD + EU"}, \code{"OECD only"}, \code{"EU only"}, and \code{"Neither OECD nor EU"}.}

\item{countries}{Optional character vector of country names to plot when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible, for example \code{"China"} for
\code{"China (People's Republic of)"}. For readability, country-level plots
are limited to 6 countries.}

\item{show_points}{Logical scalar indicating whether to draw points on top of
the line.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot the derived retirement-age measure from \link{retirement} either for OECD/EU
membership groups or for selected countries.
}
\details{
For the full variable dictionary of the underlying dataset, including
demographic variable definitions, see \link{retirement}.

When \code{level = "group"}, the function computes an unweighted mean of the
derived retirement-age measure across all countries in each OECD/EU
membership group for each year. Aggregate entries such as
\code{"European Union (27 countries)"} and \code{"OECD - Average"} are excluded before
computing group means.

When \code{level = "country"}, the function plots the derived retirement-age
measure directly for the requested countries without aggregation.

The y-axis is computed from the plotted data and always includes at least one
break below the minimum value and one break above the maximum value.

Use \code{\link[=plot_demography_trends]{plot_demography_trends()}} for demographic variables and
\code{\link[=plot_retirement_vs_demography]{plot_retirement_vs_demography()}} for paired retirement-demography plots.
}
\examples{
plot_retirement_trends()

plot_retirement_trends(
  sex = "women"
)

plot_retirement_trends(
  sex = "men"
)

plot_retirement_trends(
  level = "group",
  groups = "OECD + EU"
)

plot_retirement_trends(
  level = "group",
  groups = c("OECD + EU", "OECD only")
)

plot_retirement_trends(
  level = "country",
  countries = c("China", "Japan", "Germany")
)

}
