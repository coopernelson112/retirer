% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_demography_trends.R
\name{plot_demography_trends}
\alias{plot_demography_trends}
\title{Plot demographic trends over time}
\usage{
plot_demography_trends(
  measure,
  sex = c("both", "women", "men"),
  level = c("group", "country"),
  groups = NULL,
  countries = NULL,
  show_points = TRUE
)
}
\arguments{
\item{measure}{Character scalar naming the demographic measure to plot.
Use a numeric demographic column name from \link{retirement}, for example
\code{"MedianAgePop"}, \code{"TFR"}, \code{"PopGrowthRate"}, \code{"CNMR"}, \code{"LEx"}, or
\code{"NetMigrations"}.}

\item{sex}{Character scalar indicating the sex-specific version to use when
relevant. Must be one of \code{"both"}, \code{"women"}, or \code{"men"}. For
sex-comparable demographic measures, \code{"women"} and \code{"men"} use the
corresponding sex-specific columns and \code{"both"} uses the official
combined-series column in the dataset. For measures without sex-specific
versions, \code{sex} is accepted but does not affect the plotted values.}

\item{level}{Character scalar indicating the level to plot. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to plot when
\code{level = "group"}. If \code{NULL}, all four membership groups are plotted.}

\item{countries}{Optional character vector of country names to plot when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible. For readability, country-level plots are limited
to 6 countries.}

\item{show_points}{Logical scalar indicating whether to draw points on top of
the line.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot a demographic measure from \link{retirement} either for OECD/EU membership
groups or for selected countries.
}
\details{
For the full variable dictionary of the underlying dataset, including
demographic variable definitions, see \link{retirement}.

This function is intended for demographic measures broadly, including
variables not measured in years, such as fertility, migration, growth,
mortality rates, and population counts.

When \code{level = "group"}, the function computes an unweighted mean of the
selected measure across all countries in each OECD/EU membership group for
each year. Aggregate entries such as \code{"European Union (27 countries)"} and
\code{"OECD - Average"} are excluded before computing group means.

When \code{level = "country"}, the function plots the selected measure directly
for the requested countries without aggregation.

The y-axis is computed from the plotted data and always includes at least one
break below the minimum value and one break above the maximum value.
}
\examples{
plot_demography_trends(measure = "MedianAgePop")

plot_demography_trends(measure = "TFR")

plot_demography_trends(measure = "PopGrowthRate")

plot_demography_trends(measure = "CNMR")

plot_demography_trends(measure = "LEx", sex = "women")

plot_demography_trends(
  level = "group",
  groups = c("OECD + EU", "OECD only"),
  measure = "TFR"
)

plot_demography_trends(
  level = "country",
  countries = c("China", "Japan", "Germany"),
  measure = "PopGrowthRate"
)

}
