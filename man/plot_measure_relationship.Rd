% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_measure_relationship.R
\name{plot_measure_relationship}
\alias{plot_measure_relationship}
\title{Plot the cross-sectional relationship between two measures}
\usage{
plot_measure_relationship(
  x_measure = "LE65",
  y_measure = "retirement_age",
  year = NULL,
  x_sex = c("both", "women", "men"),
  y_sex = c("both", "women", "men"),
  level = c("group", "country"),
  groups = NULL,
  countries = NULL,
  show_labels = FALSE,
  add_smooth = TRUE
)
}
\arguments{
\item{x_measure}{Character scalar naming the measure to place on the x-axis.}

\item{y_measure}{Character scalar naming the measure to place on the y-axis.}

\item{year}{Optional numeric scalar giving the year to plot. If \code{NULL}, the
latest year with overlapping non-missing data for both measures and the
requested selection is used.}

\item{x_sex}{Character scalar indicating the sex-specific version of
\code{x_measure} to use. Must be one of \code{"both"}, \code{"women"}, or \code{"men"}.}

\item{y_sex}{Character scalar indicating the sex-specific version of
\code{y_measure} to use. Must be one of \code{"both"}, \code{"women"}, or \code{"men"}.}

\item{level}{Character scalar indicating the level to plot. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to plot when
\code{level = "group"}. If \code{NULL}, all four membership groups are plotted.}

\item{countries}{Optional character vector of country names to plot when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible. For readability, country-level selections are
limited to 6 countries.}

\item{show_labels}{Logical scalar indicating whether to label the points with
\code{series_label}.}

\item{add_smooth}{Logical scalar indicating whether to add a least-squares
regression line.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot one measure against another for a single year, either across OECD/EU
membership groups or across selected countries.
}
\examples{
plot_measure_relationship()

plot_measure_relationship(
  x_measure = "LE65",
  y_measure = "retirement_age",
  year = 2020
)

plot_measure_relationship(
  x_measure = "LE65",
  y_measure = "retirement_age",
  x_sex = "women",
  y_sex = "men",
  level = "country",
  countries = c("China", "Japan", "Germany"),
  show_labels = TRUE,
  add_smooth = FALSE
)

}
