% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_retirement_vs_demography.R
\name{plot_retirement_vs_demography}
\alias{plot_retirement_vs_demography}
\title{Plot retirement age alongside a demographic measure measured in years}
\usage{
plot_retirement_vs_demography(
  demographic_measure = "LE65",
  sex = c("both", "women", "men"),
  level = c("group", "country"),
  groups = NULL,
  countries = NULL
)
}
\arguments{
\item{demographic_measure}{Character scalar naming the demographic measure.
Must be one of \code{"MedianAgePop"}, \code{"LEx"}, \code{"LE15"}, \code{"LE65"}, \code{"LE80"},
or \code{"MAC"}.}

\item{sex}{Character scalar indicating the sex-specific version to use when
relevant. Must be one of \code{"both"}, \code{"women"}, or \code{"men"}. For retirement
age, \code{"both"} plots the row-wise average of \code{ave_age_women} and
\code{ave_age_men} because no official combined retirement-age series is
provided. For sex-comparable demographic measures, \code{"women"} and \code{"men"}
use the corresponding sex-specific columns and \code{"both"} uses the official
combined-series column in the dataset. For measures without sex-specific
versions, \code{sex} is accepted but does not affect the plotted values.}

\item{level}{Character scalar indicating the level to plot. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to plot when
\code{level = "group"}. If \code{NULL}, all four membership groups are plotted.}

\item{countries}{Optional character vector of country names to plot when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible. For readability, country-level plots are limited
to 6 countries.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot retirement age and a demographic measure measured in years in aligned
panels over time, either for OECD/EU membership groups or for selected
countries.
}
\details{
For the full variable dictionary of the underlying dataset, including
demographic variable definitions, see \link{retirement}.

This function is intended for retirement-age comparisons with demographic
measures expressed in years. Broader demographic variables such as fertility,
migration, population counts, and rates should be handled by a separate
demographic-trends function.

This function builds two aligned panels with a shared x-axis:
\itemize{
\item a retirement-age panel
\item a demographic panel
}

Each panel uses its own y-axis scale, with breaks chosen from the plotted
data so that there is at least one break below the minimum value and one
break above the maximum value.

When \code{level = "group"}, the function computes unweighted means across
countries within each OECD/EU membership group for each year. When
\code{level = "country"}, it plots the selected countries directly.
}
\examples{
plot_retirement_vs_demography()

plot_retirement_vs_demography(
  sex = "women",
  demographic_measure = "LE65"
)

plot_retirement_vs_demography(
  sex = "men",
  demographic_measure = "LEx"
)

plot_retirement_vs_demography(
  demographic_measure = "MedianAgePop"
)

plot_retirement_vs_demography(
  demographic_measure = "MAC"
)

plot_retirement_vs_demography(
  level = "group",
  groups = c("OECD + EU", "EU only")
)

plot_retirement_vs_demography(
  level = "country",
  countries = c("China", "Japan", "Germany")
)

}
