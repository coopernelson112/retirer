% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sex_gap_data.R
\name{sex_gap_data}
\alias{sex_gap_data}
\title{Return sex-gap data for a single year or the latest available year}
\usage{
sex_gap_data(
  measure = "retirement_age",
  year = NULL,
  level = c("group", "country"),
  groups = NULL,
  countries = NULL,
  gap = "women_minus_men"
)
}
\arguments{
\item{measure}{Character scalar naming the measure. Must be one of the values
returned by \code{\link[=sex_comparable_measures]{sex_comparable_measures()}}.}

\item{year}{Optional numeric scalar giving the year to return. If \code{NULL}, the
latest year with non-missing sex-gap data for the requested selection is
used.}

\item{level}{Character scalar indicating the level to return. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to return when
\code{level = "group"}. If \code{NULL}, all four membership groups are returned.}

\item{countries}{Optional character vector of country names to return when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible. For readability, country-level selections are
limited to 6 countries.}

\item{gap}{Character scalar indicating the direction of the gap. Must be one
of \code{"women_minus_men"} or \code{"men_minus_women"}.}
}
\value{
A data frame with one row per group or country for the selected year.
}
\description{
Return tidy sex-gap data for a sex-comparable measure from \link{retirement},
either for OECD/EU membership groups or for selected countries.
}
\examples{
sex_gap_data()

sex_gap_data(measure = "LE65", year = 2020)

sex_gap_data(
  measure = "retirement_age",
  level = "country",
  countries = c("China", "Japan", "Germany")
)

}
