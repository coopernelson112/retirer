% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sex_gap_snapshot.R
\name{plot_sex_gap_snapshot}
\alias{plot_sex_gap_snapshot}
\title{Plot a sex-gap snapshot for a single year}
\usage{
plot_sex_gap_snapshot(
  measure = "retirement_age",
  year = NULL,
  level = c("group", "country"),
  groups = NULL,
  countries = NULL,
  gap = "women_minus_men",
  sort = c("descending", "ascending", "none")
)
}
\arguments{
\item{measure}{Character scalar naming the measure. Must be one of the values
returned by \code{\link[=sex_comparable_measures]{sex_comparable_measures()}}.}

\item{year}{Optional numeric scalar giving the year to plot. If \code{NULL}, the
latest year with non-missing sex-gap data for the requested selection is
used.}

\item{level}{Character scalar indicating the level to plot. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to plot when
\code{level = "group"}. If \code{NULL}, all four membership groups are plotted.}

\item{countries}{Optional character vector of country names to plot when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible. For readability, country-level selections are
limited to 6 countries.}

\item{gap}{Character scalar indicating the direction of the gap. Must be one
of \code{"women_minus_men"} or \code{"men_minus_women"}.}

\item{sort}{Character scalar controlling the display order. Must be one of
\code{"descending"}, \code{"ascending"}, or \code{"none"}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot a directed sex gap from \link{retirement} for one year, either across
OECD/EU membership groups or across selected countries.
}
\examples{
plot_sex_gap_snapshot()

plot_sex_gap_snapshot(measure = "LE65", year = 2020)

plot_sex_gap_snapshot(
  measure = "retirement_age",
  level = "country",
  countries = c("China", "Japan", "Germany")
)

}
