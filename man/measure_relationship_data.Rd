% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure_relationship_data.R
\name{measure_relationship_data}
\alias{measure_relationship_data}
\title{Return paired measure data for a single year or the latest overlapping year}
\usage{
measure_relationship_data(
  x_measure = "LE65",
  y_measure = "retirement_age",
  year = NULL,
  x_sex = c("both", "women", "men"),
  y_sex = c("both", "women", "men"),
  level = c("group", "country"),
  groups = NULL,
  countries = NULL
)
}
\arguments{
\item{x_measure}{Character scalar naming the measure to place on the x-axis.}

\item{y_measure}{Character scalar naming the measure to place on the y-axis.}

\item{year}{Optional numeric scalar giving the year to return. If \code{NULL}, the
latest year with overlapping non-missing data for both measures and the
requested selection is used.}

\item{x_sex}{Character scalar indicating the sex-specific version of
\code{x_measure} to use. Must be one of \code{"both"}, \code{"women"}, or \code{"men"}.}

\item{y_sex}{Character scalar indicating the sex-specific version of
\code{y_measure} to use. Must be one of \code{"both"}, \code{"women"}, or \code{"men"}.}

\item{level}{Character scalar indicating the level to return. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to return when
\code{level = "group"}. If \code{NULL}, all four membership groups are returned.}

\item{countries}{Optional character vector of country names to return when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible. For readability, country-level selections are
limited to 6 countries.}
}
\value{
A data frame with one row per group or country for the selected year. The
returned columns are:
\describe{
\item{year}{The selected year.}
\item{series_label}{The group or country label.}
\item{x_value}{The value of \code{x_measure}.}
\item{y_value}{The value of \code{y_measure}.}
\item{x_measure}{The requested x-axis measure.}
\item{y_measure}{The requested y-axis measure.}
\item{x_sex}{The requested sex selection for \code{x_measure}.}
\item{y_sex}{The requested sex selection for \code{y_measure}.}
\item{level}{The requested level.}
}
}
\description{
Return tidy cross-sectional data pairing two measures from \link{retirement},
either for OECD/EU membership groups or for selected countries.
}
\examples{
measure_relationship_data()

measure_relationship_data(
  x_measure = "LE65",
  y_measure = "retirement_age",
  year = 2020
)

measure_relationship_data(
  x_measure = "LE65",
  y_measure = "retirement_age",
  x_sex = "women",
  y_sex = "men",
  level = "country",
  countries = c("China", "Japan", "Germany")
)

}
