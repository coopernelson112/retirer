% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sex_comparison_trends.R
\name{plot_sex_comparison_trends}
\alias{plot_sex_comparison_trends}
\title{Plot women and men together over time for a sex-comparable measure}
\usage{
plot_sex_comparison_trends(
  measure = "retirement_age",
  level = c("group", "country"),
  groups = NULL,
  countries = NULL,
  show_points = TRUE
)
}
\arguments{
\item{measure}{Character scalar naming the measure to plot. Must be one of
the values returned by \code{\link[=sex_comparable_measures]{sex_comparable_measures()}}.}

\item{level}{Character scalar indicating the level to plot. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to plot when
\code{level = "group"}. If \code{NULL}, all four membership groups are plotted.}

\item{countries}{Optional character vector of country names to plot when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible. For readability, country-level plots are limited
to 6 countries.}

\item{show_points}{Logical scalar indicating whether to draw points on top of
the lines.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot a measure from \link{retirement} for women and men side by side over time,
either for OECD/EU membership groups or for selected countries.
}
\details{
For the full variable dictionary of the underlying dataset, including
demographic variable definitions, see \link{retirement}.

This function is the women-versus-men counterpart to
\code{\link[=plot_retirement_trends]{plot_retirement_trends()}} and \code{\link[=plot_demography_trends]{plot_demography_trends()}}. It always plots
both sexes and facets by group or country so that the women and men series
can be compared directly within each panel.
}
\examples{
plot_sex_comparison_trends(measure = "retirement_age")

plot_sex_comparison_trends(measure = "LE65")

plot_sex_comparison_trends(
  measure = "LEx",
  level = "group",
  groups = c("OECD + EU", "OECD only")
)

plot_sex_comparison_trends(
  measure = "retirement_age",
  level = "country",
  countries = c("China", "Japan", "Germany")
)

}
