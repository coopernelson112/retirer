% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sex_gap_trends.R
\name{plot_sex_gap_trends}
\alias{plot_sex_gap_trends}
\title{Plot sex gaps over time for a sex-comparable measure}
\usage{
plot_sex_gap_trends(
  measure = "retirement_age",
  level = c("group", "country"),
  groups = NULL,
  countries = NULL,
  gap = "women_minus_men",
  show_points = TRUE
)
}
\arguments{
\item{measure}{Character scalar naming the measure to plot. Must be one of
the values returned by \code{\link[=sex_comparable_measures]{sex_comparable_measures()}}.}

\item{level}{Character scalar indicating the level to plot. Must be one of
\code{"group"} or \code{"country"}.}

\item{groups}{Optional character vector of group labels to plot when
\code{level = "group"}. If \code{NULL}, all four membership groups are plotted.}

\item{countries}{Optional character vector of country names to plot when
\code{level = "country"}. Country names are matched flexibly against common
aliases where possible. For readability, country-level plots are limited
to 6 countries.}

\item{gap}{Character scalar indicating the direction of the gap. Must be one
of \code{"women_minus_men"} or \code{"men_minus_women"}.}

\item{show_points}{Logical scalar indicating whether to draw points on top of
the line.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot the women-minus-men or men-minus-women gap over time for a measure from
\link{retirement}, either for OECD/EU membership groups or for selected countries.
}
\details{
For the full variable dictionary of the underlying dataset, including
demographic variable definitions, see \link{retirement}.

This function computes a directed sex gap from women and men values:
\itemize{
\item \code{"women_minus_men"} computes women - men
\item \code{"men_minus_women"} computes men - women
}

A horizontal reference line at 0 is included so that the direction and size
of the gap are easy to interpret.
}
\examples{
plot_sex_gap_trends(measure = "retirement_age")

plot_sex_gap_trends(
  measure = "LE65",
  gap = "women_minus_men"
)

plot_sex_gap_trends(
  measure = "LEx",
  level = "group",
  groups = c("OECD + EU", "EU only")
)

plot_sex_gap_trends(
  measure = "retirement_age",
  level = "country",
  countries = c("China", "Japan", "Germany")
)

}
